<!--pages/repairList/repairList.wxml-->
<view class="out">
    <scroll requesting="{{mData.requesting}}" end="{{mData.end}}" empty-show="{{mData.emptyShow}}"
        list-count="{{mData.listData.length}}" top-size="0" bottom-size="{{bottomHeight}}" bind:refresh="refresh"
        bind:more="more" style="padding-top: 0rpx">

    </scroll>
</view>