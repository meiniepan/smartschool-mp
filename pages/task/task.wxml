<!--pages/task/task.wxml-->
<view class="top-wrap">
    <tab id="category"
         tab-data="{{categoryMenu}}"
         tab-cur="{{categoryCur}}"
         size="{{80}}"
         scroll="{{true}}"
         bindchange="toggleCategory">
    </tab>
</view>

<swiper current="{{categoryCur}}" duration="{{duration}}" bindanimationfinish="animationFinish">
    <swiper-item wx:for="{{categoryData}}" wx:key="index">
        <scroll requesting="{{item.requesting}}"
                end="{{item.end}}"
                empty-show="{{item.emptyShow}}"
                list-count="{{item.listData.length}}"
                top-size="80"
                bottom-size="{{bottomHeight}}"
                bind:refresh="refresh"
                bind:more="more">
            <view class="cells">
                <view class="cell"
                      wx:for="{{item.listData}}"
                      wx:key="index"
                      data-url="{{item.id}}"
                      bindtap="doTaskDetail">
                    <view class="titles">
                        <view class="title">{{item.taskname}}</view>
                        <view class="status">{{item.statusStr}}</view>
                    </view>
                    <text class="tag1">{{item.line2Str}}</text>
                    <text class="tag">{{item.remark}}</text>
                    <text class="tag">{{item.line4Str}}</text>
                </view>
            </view>
        </scroll>
    </swiper-item>

</swiper>

