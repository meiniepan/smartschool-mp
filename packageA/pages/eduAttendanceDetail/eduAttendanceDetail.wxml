<!--packageA/pages/eduAttendanceDetail/eduAttendanceDetail.wxml-->
<view class="out">
    <view class="top-area">
        <view class="head-line" style="margin-bottom: 10rpx">
            <view class="text">{{date}}</view>
            <view class="text">{{date}}</view>
        </view>
        <view class="head-line" style="margin-bottom: 10rpx">
            <view class="text">{{date}}</view>
        </view>
        <view class="head-line" style="margin-bottom: 10rpx">
            <view class="text">{{date}}</view>
            <view class="text">{{date}}</view>
            <view class="text">{{date}}</view>
        </view>
        <view class="head-line">
            <view class="text">{{date}}</view>
            <view class="text">{{date}}</view>
            <view class="text">{{date}}</view>
        </view>
    </view>
    <view class="label-area">
        <picker bindchange="changeType"
                range="{{typeArrays}}">
            <view class="head-block">
                <view class="picker">
                    {{typeArrays[indexType]}}
                </view>
                <image src="../../assets/images/ic_gray_triangle.png" style="margin-left: 4rpx;height: 10rpx"
                       mode="heightFix" />
            </view>
        </picker>
        <view class="text">
            批量处理
        </view>
    </view>
<view class="split-line"></view>

        <view class="cells0">
            <view class="cell0 {{index%2==0?'bac0':'bac1'}}" wx:for="{{mData}}" wx:key="index" data-bean="item" bindtap="do1">
               <view class="text">aa</view>
               <view class="text">aa</view>
               <view class="text">aa</view>
            </view>
        </view>

</view>

<page-container
        show="{{show}}"
        overlay="{{overlay}}"
        close-on-slide-down
        duration="300"
        position="bottom"
        z-index="999"
        round
        bindclickoverlay="onClickOverlay"
        overlayStyle='z-index:99'
        customStyle='z-index:999'
>
    <view class="layout0">
        <view class="cells">
            <view class="cell" wx:for="{{ruleArrays}}" wx:key="index" data-index="{{index}}" bindtap="checkRule">
                <view>
                    {{item.typename}}
                </view>
                <image wx:if="{{item.checked}}" class="image" mode="heightFix"
                       src="../../assets/images/ic_check_multi.png"/>
            </view>
        </view>
        <view class="layout1">
            <view class="btn1" bindtap="doBtn1">取消</view>
            <view class="btn2" bindtap="doBtn2">确定</view>
        </view>
    </view>
</page-container>


